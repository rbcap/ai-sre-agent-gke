apiVersion: apps/v1
kind: Deployment
metadata:
  name: imagepull-demo
  namespace: ai-sre
spec:
  replicas: 1
  selector:
    matchLabels:
      app: imagepull-demo
  template:
    metadata:
      labels:
        app: imagepull-demo
    spec:
      containers:
        - name: imagepull-demo
          image: nginx:this-tag-does-not-exist-12345
